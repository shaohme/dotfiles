#!/bin/bash
# probably within emacs
if [ "$TERM" = "dumb" ]; then
	emacsclient "$@"
fi
if [ -t 1 ] ; then
    emacsclient -nw "$@"
else
    # x-terminal-emulator -e "emacsclient -nw $@"
	emacsclient -c "$@"
fi
